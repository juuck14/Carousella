# 샘플 표지 이미지

`샘플` 버튼을 누르면 `cover.jpg`가 표지 이미지로 자동 로드됩니다.

현재는 1x1 placeholder 이미지가 들어 있습니다. 실제 샘플 이미지로 교체하려면
이 폴더의 `cover.jpg` 파일을 원하는 이미지로 덮어쓰면 됩니다 (다른 파일명을 쓰려면
`frontend/src/lib/sampleData/index.js`의 `SAMPLE_IMAGE_URL`도 함께 수정).
