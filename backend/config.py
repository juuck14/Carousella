"""
config.py — 캐러셀 생성기 설정 파일
=====================================
여기만 수정하면 됩니다. generate.py는 건드리지 않아도 됩니다.
"""

import os

# =============================================================================
#  폰트
# =============================================================================

# Windows 기본 내장 Noto Serif KR (권장):
FONT_PATH_REGULAR = "C:/Windows/Fonts/NotoSerifKR-VF.ttf"
FONT_PATH_BOLD    = "C:/Windows/Fonts/NotoSerifKR-VF.ttf"

# 나눔명조 (별도 설치 필요, 더 전통적인 느낌):
# FONT_PATH_REGULAR = "./fonts/NanumMyeongjo.ttf"
# FONT_PATH_BOLD    = "./fonts/NanumMyeongjoBold.ttf"

# macOS:
# FONT_PATH_REGULAR = "/System/Library/Fonts/AppleMyungjo.ttf"
# FONT_PATH_BOLD    = "/System/Library/Fonts/AppleMyungjo.ttf"

# =============================================================================
#  캔버스
# =============================================================================

CANVAS_SIZE = 1080   # 정방형 (가로 = 세로)
DPI         = 150    # 72 이상이면 인스타그램 업로드 OK

# =============================================================================
#  여백
# =============================================================================

MARGIN = 120   # 사방 여백 (픽셀). 최소 120 권장

# =============================================================================
#  색상
# =============================================================================

# 본문 페이지
BG_COLOR       = "#F4F3EF"   # 배경: 따뜻한 아이보리
TEXT_COLOR     = "#1C1C1C"   # 텍스트: 진한 차콜
PAGE_NUM_COLOR = "#B0ADA6"   # 페이지 번호: 연한 웜그레이
RULE_COLOR     = "#D8D5CE"   # 구분선

# 표지
COVER_BG_COLOR    = "#111111"   # 상단 텍스트 영역 배경
COVER_TEXT_COLOR  = "#F4F3EF"   # 제목 텍스트
COVER_LABEL_COLOR = "#888880"   # "— 평론 —" 레이블

# =============================================================================
#  표지 레이아웃
# =============================================================================

# 상단 텍스트 영역 비율 (0.42 = 위쪽 42%, 나머지는 이미지)
COVER_SPLIT_RATIO = 0.42

COVER_TITLE_SIZE = 48   # 제목 폰트 크기
COVER_LABEL_SIZE = 20   # 레이블 폰트 크기

# =============================================================================
#  본문 디자인
# =============================================================================

BODY_FONT_SIZE = 22    # 본문 폰트 크기
LINE_SPACING   = 2.05  # 줄간격 배수 (1.0 = 폰트 크기와 동일)
PARA_SPACING   = 0.9   # 단락 사이 추가 간격 (줄간격 배수)
LETTER_SPACING = 0     # 자간 추가 픽셀 (0 = 기본값)
PAGE_NUM_SIZE  = 20    # 페이지 번호 폰트 크기

# =============================================================================
#  경로 (수정 불필요)
# =============================================================================

PROJECT_ROOT = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
IMAGES_DIR   = os.path.join(PROJECT_ROOT, "images")
INPUTS_DIR   = os.path.join(PROJECT_ROOT, "inputs")
